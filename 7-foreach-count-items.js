const names = ["Dale", "Harry", "Laura", "James", "Bobby"];

/**
 * TO DO
 * Using Array.forEach()
 * Count the number of items in the Array
 */

let namesCount = 0;

names.forEach(person => namesCount++);

console.log(namesCount);