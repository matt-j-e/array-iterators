const ages = [31,22,63,44]; // An array showing the ages of 4 people

/**
 * TO DO
 * Using Array.map() create a new array that contains the ages of the same people in 2 years
 */

const ageAfterTwoYears = ages.map(age => age + 2);

console.log(ageAfterTwoYears);